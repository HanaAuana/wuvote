extends layout

block body
  h1#page-title #{title}
  div.u-1.form-box
      div.l-box
        form.pure-form-pure-form-stacked(action="/create" method="post" accept-charset="utf-8")
          fieldset
            input(type="text" name="content" placeholder="Description")
            select(name="category")
              option(value="") Pick a Category
              option(value="General") General
              option(value="Forms") Forms
              option(value="Reports") Reports
              option(value="Themes") Themes
              option(value="Other") Other
          button.pure-button.pure-button-primary(type='submit') Add a feature
  div.pure-g
    div.u-1-4
      h4.category General
      for feature, i in features
        if feature.category == "General" && (i < 7) 
          div.item
            a.vote-link(href="/upvote/#{feature._id}" title="Vote for this feature") #{feature.votes}
            a.update-link(href="/edit/#{feature._id}" title="Update this feature") #{feature.content}
            a.vote-btn(href="/upvote/#{feature._id}" title="Vote for this feature")
    div.u-1-4
      h4.category Forms
      for feature, i in features
        if feature.category == "Forms" && (i < 7) 
          div.item
            a.vote-link(href="/upvote/#{feature._id}" title="Vote for this feature") #{feature.votes}
            a.update-link(href="/edit/#{feature._id}" title="Update this feature") #{feature.content}
            a.vote-btn(href="/upvote/#{feature._id}" title="Vote for this feature") 
    div.u-1-4
      h4.category Reports
      for feature, i in features
        if feature.category == "Reports" && (i < 7) 
          div.item
            a.vote-link(href="/upvote/#{feature._id}" title="Vote for this feature") #{feature.votes}
            a.update-link(href="/edit/#{feature._id}" title="Update this feature") #{feature.content}
            a.vote-btn(href="/upvote/#{feature._id}" title="Vote for this feature")
    div.u-1-4
      h4.category Themes
      for feature, i in features
        if feature.category == "Themes" && (i < 7) 
          div.item
            a.vote-link(href="/upvote/#{feature._id}" title="Vote for this feature") #{feature.votes}
            a.update-link(href="/edit/#{feature._id}" title="Update this feature") #{feature.content}
            a.vote-btn(href="/upvote/#{feature._id}" title="Vote for this feature")
    div.u-1-4
      h4.category Other
      for feature, i in features
        if feature.category == "Other" && (i < 7) 
          div.item
            a.vote-link(href="/upvote/#{feature._id}" title="Vote for this feature") #{feature.votes}
            a.update-link(href="/edit/#{feature._id}" title="Update this feature") #{feature.content}
            a.vote-btn(href="/upvote/#{feature._id}" title="Vote for this feature")
    
        
